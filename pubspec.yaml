name: amasyasondakika
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5

  cupertino_icons: ^1.0.8
  cached_network_image: ^3.3.1

  # ✅ Runtime dependencies (dev_dependencies altında olmamalı)
  firebase_core: ^3.8.0
  cloud_firestore: ^5.5.0
  http: ^1.1.0
  webfeed_plus: ^1.1.2
  webview_flutter: ^4.8.0
  url_launcher: ^6.2.6
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  firebase_messaging: ^15.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/sondakika.jpg
